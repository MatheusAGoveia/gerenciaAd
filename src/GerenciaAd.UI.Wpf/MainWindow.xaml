<Window x:Class="GerenciaAd.UI.Wpf.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Renovação de Conta no Active Directory" 
        Height="700" Width="900"
        WindowStartupLocation="CenterScreen"
        FontFamily="Segoe UI"
        FontSize="13"
        Background="#1E1E1E">
    
    <Window.Resources>
        <!-- Cores do tema -->
        <SolidColorBrush x:Key="BackgroundMain" Color="#1E1E1E"/>
        <SolidColorBrush x:Key="BackgroundSection" Color="#252526"/>
        <SolidColorBrush x:Key="TextPrimary" Color="#FFFFFF"/>
        <SolidColorBrush x:Key="TextSecondary" Color="#CCCCCC"/>
        <SolidColorBrush x:Key="ButtonPrimary" Color="#0078D4"/>
        <SolidColorBrush x:Key="ButtonHover" Color="#106EBE"/>
        <SolidColorBrush x:Key="BorderColor" Color="#3F3F46"/>
        
        <!-- Estilos para GroupBox -->
        <Style TargetType="GroupBox">
            <Setter Property="Margin" Value="0,0,0,15"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="GroupBox">
                        <Border Background="{StaticResource BackgroundSection}"
                                BorderBrush="{StaticResource BorderColor}"
                                BorderThickness="1"
                                CornerRadius="8"
                                Padding="15">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>
                                <Border Grid.Row="0"
                                        Background="{StaticResource BackgroundSection}"
                                        Margin="0,0,0,10">
                                    <ContentPresenter ContentSource="Header"
                                                      Margin="0"/>
                                </Border>
                                <ContentPresenter Grid.Row="1" ContentSource="Content"/>
                            </Grid>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <!-- Estilos para Label -->
        <Style TargetType="Label">
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Padding" Value="0,8"/>
            <Setter Property="MinWidth" Value="150"/>
            <Setter Property="Foreground" Value="{StaticResource TextSecondary}"/>
            <Setter Property="FontSize" Value="13"/>
        </Style>
        
        <!-- Estilos para TextBox -->
        <Style TargetType="TextBox">
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Padding" Value="8"/>
            <Setter Property="MinHeight" Value="32"/>
            <Setter Property="Background" Value="#2D2D30"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderColor}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6"
                                Padding="{TemplateBinding Padding}">
                            <ScrollViewer x:Name="PART_ContentHost"
                                        VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <!-- Estilos para ComboBox -->
        <Style TargetType="ComboBox">
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Padding" Value="8"/>
            <Setter Property="MinHeight" Value="32"/>
            <Setter Property="Background" Value="#2D2D30"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderColor}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontSize" Value="13"/>
        </Style>
        
        <!-- Estilos para Button -->
        <Style TargetType="Button">
            <Setter Property="MinWidth" Value="130"/>
            <Setter Property="MinHeight" Value="36"/>
            <Setter Property="Padding" Value="20,8"/>
            <Setter Property="Margin" Value="10,0,0,0"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Background" Value="{StaticResource ButtonPrimary}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="6"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center"
                                            VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{StaticResource ButtonHover}"/>
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Background" Value="#3F3F46"/>
                    <Setter Property="Foreground" Value="#808080"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        
        <!-- Estilos para TextBlock -->
        <Style TargetType="TextBlock">
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Padding" Value="0,8"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
            <Setter Property="FontSize" Value="13"/>
        </Style>
    </Window.Resources>
    
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Cabeçalho -->
        <Border Grid.Row="0" 
                Background="#252526" 
                Padding="20,15" 
                Margin="0,0,0,20"
                CornerRadius="8"
                BorderBrush="{StaticResource BorderColor}"
                BorderThickness="1">
            <TextBlock Text="Renovação de Conta no Active Directory" 
                       FontSize="18" 
                       FontWeight="Bold"
                       Foreground="{StaticResource TextPrimary}"/>
        </Border>

        <!-- Seção 1: Consulta -->
        <GroupBox Grid.Row="1" Header="Consulta">
            <Grid Margin="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Label Grid.Row="0" Grid.Column="0" Content="Login (sAMAccountName):"/>
                <TextBox Grid.Row="0" Grid.Column="1" 
                         Text="{Binding Login, UpdateSourceTrigger=PropertyChanged}" 
                         Margin="10,5,0,5"/>

                <Label Grid.Row="1" Grid.Column="0" Content="Domínio:"/>
                <ComboBox Grid.Row="1" Grid.Column="1" 
                          ItemsSource="{Binding DominiosDisponiveis}"
                          SelectedItem="{Binding DominioSelecionado}"
                          Margin="10,5,0,5"/>

                <Button Grid.Row="0" Grid.RowSpan="2" Grid.Column="2"
                        Content="Buscar" 
                        Command="{Binding BuscarCommand}"
                        VerticalAlignment="Center"/>
            </Grid>
        </GroupBox>

        <!-- Seção 2: Informações do Usuário -->
        <GroupBox Grid.Row="2" Header="Informações do Usuário">
            <Grid Margin="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Label Grid.Row="0" Grid.Column="0" Content="Nome Exibido:"/>
                <TextBlock Grid.Row="0" Grid.Column="1" 
                           Text="{Binding UsuarioAtual.DisplayName}" 
                           Margin="10,5,0,5"
                           Foreground="{StaticResource TextPrimary}"/>

                <Label Grid.Row="1" Grid.Column="0" Content="UPN:"/>
                <TextBlock Grid.Row="1" Grid.Column="1" 
                           Text="{Binding UsuarioAtual.UserPrincipalName}" 
                           Margin="10,5,0,5"
                           Foreground="{StaticResource TextPrimary}"/>

                <Label Grid.Row="2" Grid.Column="0" Content="Data de Expiração:"/>
                <TextBlock Grid.Row="2" Grid.Column="1" 
                           Text="{Binding DataExpiracaoFormatada}" 
                           Margin="10,5,0,5"
                           Foreground="{StaticResource TextPrimary}"/>

                <Label Grid.Row="3" Grid.Column="0" Content="Status:"/>
                <TextBlock Grid.Row="3" Grid.Column="1" 
                           Text="{Binding StatusFormatado}" 
                           Margin="10,5,0,5"
                           Foreground="{StaticResource TextPrimary}"/>
            </Grid>
        </GroupBox>

        <!-- Seção 3: Renovação -->
        <GroupBox Grid.Row="3" Header="Renovação">
            <Grid Margin="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <Label Grid.Column="0" Content="Tipo de Contrato:"/>
                <ComboBox Grid.Column="1" 
                          ItemsSource="{Binding TiposContratoDisponiveis}"
                          SelectedItem="{Binding TipoContratoSelecionado}"
                          Margin="10,5,0,5"/>

                <Button Grid.Column="2"
                        Content="Renovar" 
                        Command="{Binding RenovarCommand}"/>
            </Grid>
        </GroupBox>

        <!-- Seção: Nova Data de Expiração (após renovação) -->
        <GroupBox Grid.Row="4" Header="Nova Data de Expiração">
            <GroupBox.Style>
                <Style TargetType="GroupBox">
                    <Setter Property="Visibility" Value="Collapsed"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding MostrarNovaData}" Value="True">
                            <Setter Property="Visibility" Value="Visible"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </GroupBox.Style>
            <Border Background="#1E3A1E" 
                    Padding="15" 
                    Margin="0"
                    CornerRadius="6"
                    BorderBrush="#2E7D32"
                    BorderThickness="1">
                <TextBlock Text="{Binding NovaDataFormatada}" 
                           FontWeight="Bold"
                           FontSize="14"
                           Foreground="#81C784"/>
            </Border>
        </GroupBox>

        <!-- Seção 4: Mensagens -->
        <GroupBox Grid.Row="5" Header="Mensagens">
            <ScrollViewer VerticalScrollBarVisibility="Auto" 
                          MaxHeight="130"
                          Padding="0">
                <Border Background="#2D2D30" 
                        Padding="12" 
                        Margin="0"
                        CornerRadius="6"
                        BorderBrush="{StaticResource BorderColor}"
                        BorderThickness="1">
                    <TextBlock Text="{Binding Mensagem}" 
                               TextWrapping="Wrap" 
                               Foreground="{Binding CorMensagem}"
                               FontSize="13"
                               LineHeight="20"/>
                </Border>
            </ScrollViewer>
        </GroupBox>

        <!-- Status Bar -->
        <StatusBar Grid.Row="6" 
                   VerticalAlignment="Bottom"
                   Background="#252526"
                   BorderBrush="{StaticResource BorderColor}"
                   BorderThickness="0,1,0,0"
                   Height="28">
            <StatusBarItem>
                <TextBlock Text="{Binding StatusBarMensagem}" 
                           FontSize="12"
                           Foreground="{StaticResource TextSecondary}"
                           Margin="5,0"/>
            </StatusBarItem>
        </StatusBar>
    </Grid>
</Window>
